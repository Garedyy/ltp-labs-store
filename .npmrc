engine-strict=true
# accessibility-checker depends on chromedriver, whose postinstall downloads a 17 MB binary we never
# use (we scan Playwright pages). chromedriver reads this npm config; puppeteer is handled by the
# "puppeteer.skipDownload" key in package.json. npm warns about the unknown key - expected.
chromedriver_skip_download=true
